akka {
  loglevel = "DEBUG"
  actor {
    provider = "local"
    debug {
      receive = on
      autoreceive = on
      lifecycle = on
    }
  }
  minesweeper {
    systemName = "minesweeperTest"
    controllerActor = "controllerTest"
    publisherActor = "publisherTest"
  }
}